name: Generate Podcast feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repos
      uses: actions/checkout@v3 
    - name: Run feed generator
      uses: arushsoel/podcast-generator@main
      
